name: Zorbas hero asset maintenance

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  info:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Final Zorbas homepage hero is installed.'
